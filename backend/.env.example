NODE_ENV=development
PORT=5001
FRONTEND_URL=http://localhost:3000

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?appName=Cluster0

ANTHROPIC_API_KEY=sk-ant-your-key-here

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_FROM=HEROY Digital Solutions <hello@heroy.dev>
EMAIL_TO=your-inbox-for-leads@example.com

# Long random string — required to access GET /api/contact and
# PATCH /api/contact/:id/status. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=replace-with-a-long-random-string